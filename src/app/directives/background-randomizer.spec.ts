import { BackgroundRandomizer } from './background-randomizer';

describe('BackgroundRandomizer', () => {
  it('should create an instance', () => {
    const directive = new BackgroundRandomizer();
    expect(directive).toBeTruthy();
  });
});
