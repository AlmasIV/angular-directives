import { HideOnDoubleClick } from './hide-on-double-click';

describe('HideOnDoubleClick', () => {
  it('should create an instance', () => {
    const directive = new HideOnDoubleClick();
    expect(directive).toBeTruthy();
  });
});
